/**
 * dashboard.server.test.ts — unit tests for the dashboard loader.
 *
 * Tests: contributions field returned per member in the loader response.
 * All tests are stubs (todo).
 */

import { describe, it } from "vitest";

describe("Dashboard loader contributions", () => {
  it.todo("returns contributions field for each member in the loader response");
  it.todo("parses contributions JSON string into a typed object");
  it.todo("returns null contributions for members with no contribution data");
});
